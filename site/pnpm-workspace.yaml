# Marks site/ as its own pnpm root so installs here don't resolve against
# the repo root's pnpm-workspace.yaml. The package itself is consumed via
# the file:.. dependency, not a pnpm workspace link.
